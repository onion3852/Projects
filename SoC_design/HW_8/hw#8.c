#include <stdio.h>
#include <SSDC.h>

int main(void)
{
    int global_time = 0;

    // array of each device
    int sram = {};
    int dram = {};
    int nand = {};

    // fscanf;

    while (1)
    {
        
    }
    
    return 0;
}